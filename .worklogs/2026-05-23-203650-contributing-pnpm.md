# CONTRIBUTING.md: npm → pnpm

Repo migrated to pnpm long ago (`packageManager: pnpm@11.2.2`). CONTRIBUTING.md dev-setup section still said `npm install` / `npm run build` / `npm run validate`. Updated to `pnpm` and added a one-line corepack pointer so new contributors know how the version pin is enforced.
